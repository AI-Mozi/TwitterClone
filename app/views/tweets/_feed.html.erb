<% if user_signed_in? %>
  <article class="media box">
    <figure class="media-left">
      <p class="image is-64x64">
        <%= gravatar_image_tag(current_user.email, size: 64, alt: current_user.name) %>
      </p>
    </figure>
    <div class="media-content">
      <%= render 'tweets/form' %>
    </div>
  </article>
<% end %>

  <% if @tweets.present? %>
  <% @tweets.each do |tweet| %>
  <div class="box">
    <article class="media">
      <div class="media-left">
        <figure class="image is-64x64">
          <%= gravatar_image_tag(tweet.user.email, size: 64, alt: tweet.user.name) %>
        </figure>
      </div>
      <div class="media-content">
        <div class="content">
          <strong><%= tweet.user.name %></strong>
          <small>@<%= tweet.user.username %></small>
          <small><%= (tweet.created_at).strftime("%d %B")%></small></br>
          <p><%= tweet.tweet %></p>
        </div>
        <% if user_signed_in? %>
          <nav class="level">
            <div class="level-left">
              <%= link_to tweet, class: "level-item" do %>
              <span class="icon"><i class="fa fa-comment" aria-hidden="true"></i><%= tweet.comments.count %></span>
              <% end %>
              <p class="parent-likes">
                <% if current_user.liked? tweet %>
                <%= link_to unlike_tweet_path(tweet), method: :put, class: 'unlike_tweet' do %>
                <span class="icon"><i class="fa fa-heart has-text-danger"></i></span>
                <span class="vote_count"><%= tweet.get_likes.size %></span>
                <%end%>
                <%else%>
                <%= link_to like_tweet_path(tweet), method: :put, class: 'like_tweet' do %>
                <span class="icon"><i class="fa fa-heart"></i></span>
                <span class="vote_count"><%= tweet.get_likes.size %></span>
                <%end%>
                <%end%>
              </p>
            </div>
            <% if tweet.user_id == current_user.id %>
            <div class="level-right ">
              <%= link_to edit_tweet_path(tweet), class: "level-item" do %>
              <span class="icon"><i class="fa fa-pencil" aria-hidden="true"></i></span>
              <% end %>
              <%= link_to tweet, class: "level-item", method: :delete , data: {confirm: "Are you sure you want to delete this tweet? "} do%>
              <span class="icon"><i class="fa fa-trash-o" aria-hidden="true"></i></span>
              <% end %>
            </div>
            <% end %>
          </nav>
        <% else %>
        <nav class="level">
          <div class="level-left">
            <%= link_to tweet, class: "level-item" do %>
              <span class="icon"><i class="fa fa-comment" aria-hidden="true"></i><%= tweet.comments.count %></span>
              <% end %>
            <span class="icon"><i class="fa fa-heart has-text-danger heart"></i><%= tweet.get_likes.size %></span>
          </div>
          <% end %>
      </div>
    </article>
  </div>
  <% end %>
<% else %>
  <div class="box has-text-centered">
    <p class="is-size-1">NO TWEETS :(</p>
  </div>
<% end %>